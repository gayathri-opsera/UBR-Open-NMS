{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ubr-nms.airtel.internal/schemas/kafka/raw-alarms.schema.json",
  "title": "RawAlarmMessage",
  "description": "Kafka message on the raw-alarms topic — ingested directly from devices via SNMP trap or syslog.",
  "type": "object",
  "required": ["alarmId", "alarmName", "severity", "state", "Time", "data"],
  "properties": {
    "alarmId":          { "type": "string" },
    "alarmName":        { "type": "string" },
    "severity":         { "type": "string", "enum": ["CRITICAL", "MAJOR", "MINOR", "WARNING", "INDETERMINATE", "CLEARED"] },
    "alarmDescription": { "type": "string" },
    "state":            { "type": "string", "enum": ["RAISED", "CLEARED"] },
    "Time":             { "type": "string", "format": "date-time" },
    "data": {
      "type": "object",
      "required": ["deviceType", "deviceId"],
      "properties": {
        "deviceType": { "type": "string", "enum": ["BTS", "CPE", "IDU"] },
        "deviceId":   { "type": "string" }
      }
    }
  }
}
