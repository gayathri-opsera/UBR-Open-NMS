apiVersion: apps/v1
kind: Deployment
metadata:
  name: diagnostics-service
  labels:
    app: diagnostics-service
    tenant: opsera
spec:
  replicas: 1
  selector:
    matchLabels:
      app: diagnostics-service
  template:
    metadata:
      labels:
        app: diagnostics-service
        tenant: opsera
    spec:
      serviceAccountName: ubr-nms
      imagePullSecrets:
        - name: ecr-pull-secret
      containers:
        - name: diagnostics-service
          image: PLACEHOLDER_DIAGNOSTICS_SERVICE_ECR_URI
          ports:
            - containerPort: 8090
          env:
            - name: REDIS_HOST
              value: "redis.opsera-ubr-nms-dev.svc.cluster.local"
            - name: REDIS_PORT
              value: "6379"
            - name: MONGO_URI
              value: "mongodb://mongodb.opsera-ubr-nms-dev.svc.cluster.local:27017/ubr_nms"
            - name: KAFKA_BROKERS
              value: "kafka.opsera-ubr-nms-dev.svc.cluster.local:9092"
            - name: KAFKA_ENABLED
              value: "true"
            - name: ENV
              value: "dev"
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8090
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8090
            initialDelaySeconds: 90
            periodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 100
        fsGroup: 100
