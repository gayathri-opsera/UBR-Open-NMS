apiVersion: v1
kind: Service
metadata:
  name: mycom-forwarder
  labels:
    app: mycom-forwarder
    tenant: opsera
spec:
  selector:
    app: mycom-forwarder
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
