apiVersion: v1
kind: Service
metadata:
  name: syslog-forwarder
  labels:
    app: syslog-forwarder
    tenant: opsera
spec:
  selector:
    app: syslog-forwarder
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
