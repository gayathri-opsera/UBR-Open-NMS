apiVersion: v1
kind: Service
metadata:
  name: config-push-worker
  labels:
    app: config-push-worker
    tenant: opsera
spec:
  selector:
    app: config-push-worker
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
