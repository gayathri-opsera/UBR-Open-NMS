apiVersion: v1
kind: Service
metadata:
  name: alarm-service
  labels:
    app: alarm-service
    tenant: opsera
spec:
  selector:
    app: alarm-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  type: ClusterIP
