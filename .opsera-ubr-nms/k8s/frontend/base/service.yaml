apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: frontend
    tenant: opsera
spec:
  selector:
    app: frontend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
