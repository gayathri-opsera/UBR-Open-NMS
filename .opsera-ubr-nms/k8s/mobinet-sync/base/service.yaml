apiVersion: v1
kind: Service
metadata:
  name: mobinet-sync
  labels:
    app: mobinet-sync
    tenant: opsera
spec:
  selector:
    app: mobinet-sync
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
