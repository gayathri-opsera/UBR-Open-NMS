apiVersion: apps/v1
kind: Deployment
metadata:
  name: mobinet-sync
  labels:
    app: mobinet-sync
    tenant: opsera
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mobinet-sync
  template:
    metadata:
      labels:
        app: mobinet-sync
        tenant: opsera
    spec:
      serviceAccountName: ubr-nms
      imagePullSecrets:
        - name: ecr-pull-secret
      containers:
        - name: mobinet-sync
          image: PLACEHOLDER_MOBINET_SYNC_ECR_URI
          ports:
            - containerPort: 8080
          env:
            - name: ENV
              value: "dev"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 100
        fsGroup: 100
