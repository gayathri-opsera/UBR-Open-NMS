apiVersion: v1
kind: Service
metadata:
  name: kpi-collector
  labels:
    app: kpi-collector
    tenant: opsera
spec:
  selector:
    app: kpi-collector
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
