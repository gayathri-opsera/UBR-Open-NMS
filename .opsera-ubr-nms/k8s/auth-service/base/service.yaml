apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: auth-service
    tenant: opsera
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001
  type: ClusterIP
